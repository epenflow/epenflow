'use client'
import React from 'react'
import tw, { styled } from 'twin.macro'

const NavWrapper = styled.nav(tw`bg-black`)
const Navbar = () => {
  return <NavWrapper>Navbar</NavWrapper>
}

export default Navbar
